---
title: FaceHub
---

# 💬 FaceHub

> 端到端加密的聊天 / 朋友圈 / 小程序平台，纯前端，数据全在你自己的 GitHub 仓库里。

- 在线：<https://cool-zimo.github.io/FaceHub/>
- 仓库：`Cool-zimo/FaceHub`

---

## 加密原理

| 层 | 算法 |
|---|---|
| 密钥协商 | **X25519**（ECDH） |
| 对称加密 | **AES-GCM** |
| 密钥存储 | 公钥挂在身份仓库，私钥只在你本机浏览器 |

**私聊**：双方各有一对长期身份密钥。发消息时用「对方公钥 + 自己私钥」
做 ECDH 得到共享密钥，再用 AES-GCM 加密。公钥是公开的（挂在 `pk.json`），
私钥永远不上传。

**群聊**：用一把群密钥。成员变动（加人/踢人）时**自动轮换**，
被踢的人无法解密之后的新消息。

⚠️ **前向安全性**：换设备后旧消息解不开 —— 因为私钥留在原设备。
这是设计取舍，不是 bug。换设备需要重新生成密钥对。

---

## 仓库布局

| 仓库 | 作用 |
|---|---|
| `facehub-{你的登录名}` | 身份仓库：公钥、昵称、头像、朋友圈帖子 |
| `fbdm-{A}-{B}`（私有） | A 和 B 的私聊会话 |
| `fhgrp-{owner}-{id}`（私有） | 群聊：群密钥、成员、消息 |

消息以 **issue comment** 形式存储，每条消息一个 comment，
内容是密文（`E2E1.<base64>.<base64>` 格式）。

---

## 功能

### 聊天

私聊、群聊、附件（图片/视频/音频/文件）、滚动位置记忆。

**滚动位置恢复**有个坑：消息是异步解密的，如果只设一次 `scrollTop`，
此时 `scrollHeight` 还很矮，浏览器会把你夹回顶部。
所以实现成**重试式** —— 每 120ms 试一次，最多 20 次；你要是自己滚了，
它立刻让位。

### 朋友圈

- **关系自动派生**：私聊对象、群成员自动可见，**不用手动关注**
  （都已经在聊天了，再点一次关注是多余的）
- 关系结果缓存 10 分钟，上限 30 人 —— 否则每刷一次都要读所有群的
  `group.json`，群一多就爆了
- 发帖子：九宫格（最多 9 张）、谁可以看、所在位置
- 评论支持**配图**（最多 3 张）
- 评论回复：点任意一条评论即可回复那个人

### 小程序

仓库名形如 `fhapp-*` 的会被识别成小程序，可搜索、可加载、有历史记录。

### AI 自动回复

在「我 → AI 自动回复」里配置：智谱 Key、模型、人设、延迟。

⚠️ **两边都开会无限互聊。** 实测过：3~7 秒一条，一小时能刷几千条。
所以加了三重防护：

| 机制 | 默认 | 说明 |
|---|---|---|
| 连续上限 | 3 条 | 回满就停，真人插话才继续 |
| 冷却 | 20 秒 | 同一会话两次回复的最小间隔 |
| 429 立刻停 | — | 被限流直接放弃，不硬顶 |

只在**页面开着时**生效 —— 没有服务端，纯靠轮询。

---

## 令牌权限

需要 `repo`（读写私有会话仓库）。建议用 fine-grained token，
只勾 `Contents` 读写。

⚠️ **这个 token 能读到你的全部私聊记录**（虽然是密文）。用完建议 revoke。

---

## 已知限制

- 换设备 = 新密钥 = 旧消息解不开
- 离线不可用（数据都在 GitHub）
- 朋友圈评论是平铺的，不是微信那种嵌套折叠

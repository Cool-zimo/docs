---
title: 设计决策记录
---

# 📐 设计决策记录

> 记录**为什么这么做**，而不只是"做了什么"。
> 每条都包含被否决的方案和理由。

---

## D1 · 消息为什么存在 issue comment，而不是文件

**决定**：一个会话 = 一个私有仓库 + 一个 issue，每条消息一个 comment。

**被否决**：把消息存进 JSON 文件数组。

**理由**：

| 方案 | 并发 | 时间戳 | 增量拉取 |
|---|---|---|---|
| 文件数组 | ✗ 每次写要带 `sha`，并发必 409 | 要自己维护 | 要全量读 |
| **comment** | ✓ 追加语义天然并发 | ✓ 自带 | ✓ `since` 参数 |

**代价**：删除 comment 不真正删除（Git 历史仍在）。

---

## D2 · 为什么用 P-256 而不是 X25519

**决定**：ECDH 用 **P-256**。

**理由**：WebCrypto 原生支持 `namedCurve: 'P-256'`。
X25519 在 WebCrypto 里支持不完整（各浏览器实现不一），要引第三方库。

**代价**：P-256 性能略低、公钥略长。对这个场景无所谓。

> ⚠️ 早期文档误写为 X25519。实际源码是 P-256。两者公钥格式不同，
> 混用会导致永远协商不出相同密钥。

---

## D3 · 长期身份密钥 vs 按会话密钥

**决定**：每人一把**长期身份密钥**，公钥发布到公开仓库 `pk.json`。

**被否决**：每个会话单独生成密钥对。

**理由**：按会话生成的话，对方必须先打开应用、把公钥写进那个会话仓库，
我才能加密——**所以永远"要等双方上线"**。这是个真实的可用性缺陷。

改成长期身份密钥后，任何人想给我发消息，直接读我公开仓库的 `pk.json` 即可，
**我不需要在线**。

这就是 Signal prekey / PGP 公钥服务器的思路。

**代价**：公钥是长期固定的，前向安全性弱于"每条消息换密钥"的方案。

---

## D4 · 备份为什么是明文的

**决定**：`exportBackup()` 导出**不加密**的私钥 JSON。

**被否决**：用一段口令加密备份。

**理由**：用口令加密会引入一个新问题——**忘了口令更惨**。
用户本来就因为"换设备解不开"才需要备份，再套一层口令等于把风险换个地方。

**代价**：备份文件谁拿到谁就能解密。**所以界面必须明说这一点。**

---

## D5 · 分片大小为什么是 2MB（FaceHub）/ 512KB（Drive）

**决定**：FaceHub 2MB，Drive 512KB。

**理由（FaceHub）**，来自实测：

```
1MB ✓ 7.8s   10MB ✓ 9.3s   15MB ✓ 11.3s   25MB ✓ 12.8s
40MB ✗ 422 "file is too large to be processed"
```

单次 PUT 安全线在 30MB。但分片的价值不只是突破上限：

> 10MB 一次请求要 9.3 秒，中途抖一下全废；
> 切成 2MB 五片，每片约 2 秒，失败只重传那一片。

**单片太大 = 重试代价太高。** 取 2MB（base64 后 2.7MB）。

**理由（Drive）**：场景是大量小文件，需要更细的重试粒度，所以更小。

---

## D6 · 朋友圈为什么不用手动关注

**决定**：私聊对象 + 群成员**自动可见**。

**被否决**：照搬"关注"模型，让用户手动点。

**理由**：都已经在聊天了，凭什么还要再点一次关注？**微信也没有"关注好友"这个动作。**

手动关注保留下来，只用于关注陌生人。

**配套**：关系结果缓存 10 分钟、上限 30 人。不缓存的话每刷一次朋友圈
都要读所有群的 `group.json`，群一多请求数就爆了。

---

## D7 · 评论的图存哪里

**决定**：存**评论者自己**仓库的 `m/` 下，不是帖子作者的仓库。

**被否决**：存到帖子作者仓库。

**理由**：

1. 评论别人的帖子不该需要对方仓库的写权限——否则评论功能就废了
2. 只存路径引用，不存 base64（否则一条带图评论是几 MB 的 JSON）

⚠️ **取图 URL 必须用评论者的 login**，不是帖子作者的。用错会 404。

---

## D8 · 为什么不引 KaTeX / highlight.js

**决定**：数学公式和代码高亮都**自研**，零外部依赖。

**被否决**：引 KaTeX / MathJax / highlight.js / Prism。

**理由**：这些库要加载 CSS + **好几套字体**。网络一卡，公式就变成一堆乱码或空白——
**比不渲染还难看**。

而整个项目的原则是「零外部资源」（见 D9）。

**代价**：覆盖度不如成熟库（矩阵、复杂 LaTeX 不支持）。
**取舍**：不支持的部分**原样保留**，至少不会变成看不懂的东西。

---

## D9 · 为什么零外部依赖

**决定**：不引任何第三方库（无框架、无构建、无 CDN）。

**理由**：

1. Pages 直接托管，不需要 build
2. 改完推上去就生效，没有"构建产物和源码不同步"的风险
3. 外部 CDN 随时可能挂/被墙，依赖它等于把可用性交给别人

**代价**：`app.js` 3828 行偏大；要自己实现 base64、markdown、数学、高亮。

---

## D10 · AI 回复为什么防循环，判据是什么

**决定**：连续上限 3 条 + 冷却 20s + 429 立刻停。

**理由**：两边都开自动回复 = A 回 B、B 回 A，死循环。实测 3~7 秒一条。

**清零判据（两个坑都踩过）**：

```
✗ 看"最后一条是不是我发的"
  → 循环时 target 一直是对方的，永远不成立

✗ 放在上限判断之后
  → chain 满了直接 return，永远走不到清零，功能等于没有

✓ 放在上限判断之前，判据是"上次自动回复之后我有没有发过消息"
```

---

## D11 · 为什么不用框架

见 D9。补充一点：三个应用代码量在 8k~10k 行，框架带来的组织收益
小于它引入的构建复杂度。

---

## D12 · 仓鼠的配置为什么存仓库而不是 localStorage

**决定**：管理列表存在**私有仓库** `cangshu-config`。

**理由**：换台设备、换个浏览器就全丢了。存仓库才能多端同步。

这是三个应用的统一思路（Drive 的 config-sync 同理）。

**配套**：每次保存前把当前版本另存一份**历史快照**，改错了能回滚。

---

## D13 · 群聊移除成员为什么是那个顺序

**决定**：五步，撤权限必须放最后。

```
① 写进成员列表 → ② 分发群密钥 → ③ 轮换 → ④ 写回列表 → ⑤ 撤 collaborator
```

**理由**：

- ⑤ 放最后：前四步都需要写权限，先撤就全做不了
- ① 在分发之前：对方没公钥时分发会失败，先把人写进列表至少状态一致
- ③ 轮换：被移除者解不开之后的**新**消息（旧的他还能解，这是必然的）
- **轮换失败不阻断移除**——至少他已失去仓库访问权限

**为什么轮换现在可行**：因为身份公钥始终可读（公开仓库）。
早期设计读不到对方公钥，轮换做不了。

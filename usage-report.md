# GitHub Drive 真实使用数据报告

**数据截止**：2026-09-26
**样本**：`Cool-zimo` 主账号（网页版约 1 周 + 桌面版 1~2 天），`Feng-zimo` 副账号
**数据来源**：`github-drive-config` 仓库的 `config.json`，以及 4 个存储仓库的 Git tree API

★ 本报告只做技术性统计。涉及的个人文件内容已泛化处理，不复述文件名。

---

## 一、总览

| 指标 | 数值 |
|---|---|
| 文件数（VFS 记录） | 15 个 |
| 目录数 | 7 个（含 `.trash`） |
| VFS 记录的文件总量 | **77.0 MB** |
| 4 个存储仓库实际总量 | **133.3 MB** |
| ★ 孤儿数据（无 VFS 记录） | **56.2 MB，占 42%** |
| 分片总数 | 136 片 |
| 分享 | 4 个（其中 1 个为空壳） |
| 收藏 | 0 |

**一句话结论**：存进去的东西里，**将近一半是拿不回来的死数据**。

---

## 二、★ 核心问题：42% 是孤儿数据

### 实测

| 仓库 | 实际 blob 大小 | VFS 指向它的分片 | 判定 |
|---|---|---|---|
| `drive-storage-2026-08-30-8696` | 77.1 MB | 136 片（全部） | 在用 |
| `drive-storage-2026-08-27-c4aa` | 40.9 MB | **0** | 全部孤儿 |
| `drive-storage-2026-08-25-ft3j` | 14.2 MB | **0** | 全部孤儿 |
| `drive-storage-2026-08-25-hu15` | 1.1 MB | **0** | 全部孤儿 |
| `drive-storage-2026-08-27-2233` | 0 B | 0 | 空仓 |

后三个仓库共 **56.2 MB 没有任何 VFS 记录** —— 界面上看不到，也下载不了，但一直占着容量和仓库配额。

### 根因（两个独立原因）

**① `subtractFromRepoUsage()` 定义了，但全项目零调用**

```javascript
// storage.js:703  定义了这个方法
subtractFromRepoUsage(owner, repo, sizeBytes) { ... }

// 全项目搜索结果：
//   addToRepoUsage      → 2 处（定义 + 上传时调用）
//   subtractFromRepoUsage → 0 处调用   ← 只有定义，没人用
```

删除文件时分片确实被删了（`file-manager.js:429` 调 `api.deleteFile`），
**但记账永远只增不减**。

**② 分片删除失败只 `console.warn`，不重试**

```javascript
} catch (e) {
    console.warn(`[FileManager] 删除分片失败: ${chunk.path}`, e.message);
}
```

`sha` 过期、网络抖动、限流都会让某个分片删不掉，然后**静默留下**。
136 片里漏几片，累积几次就是几十 MB。

### 为什么界面上看不出来

孤儿数据不在 VFS 里，所以文件列表干净；但容量条是按 `repoUsage` 算的，
而记账本身也是错的（见下）——**两边都不可信，反而互相"印证"出一个看似正常的数**。

---

## 三、★ 记账与实际双向脱节

| 仓库 | 记账 | 实际 | 偏差 | 方向 |
|---|---|---|---|---|
| `…-8696` | 76.4 MB | 77.1 MB | −0.7 MB | 低估 |
| `…-c4aa` | 44.6 MB | 40.9 MB | +3.7 MB | 虚高 |
| `…-ft3j` | 6.2 MB | 14.2 MB | **−8.0 MB** | ★ 低估 |
| `…-hu15` | 0.5 MB | 1.1 MB | −0.6 MB | 低估 |

**低估比虚高危险得多。**

`pick_repo()` 靠记账判断"这个仓库还有没有空间"。ft3j 记账 6.2 MB，
实际已经 14.2 MB —— 系统**以为还空着 894 MB**，会继续往里写。
低估不会报错，只会等到某次 API 返回"仓库超限"才暴露，而那时已经写了一堆。

**虚高**的成因清楚（删了文件不扣减）；
**低估**的成因是记账数据在历史上丢过（config 重置/迁移/多设备覆盖），
上传产生了真实占用但记账条目随之丢失。两个方向叠加，让记账彻底失去参考价值。

---

## 四、僵尸记账条目

`repoUsage` 有 10 条，`repos` 只有 5 条。差出来的 5 条全是 `Feng-zimo` 的存储仓：

```
Feng-zimo/drive-storage-2026-08-30-12fa    0 B
Feng-zimo/drive-storage-2026-08-30-ffb2    0 B
Feng-zimo/drive-storage-2026-08-30-2a45    0 B
Feng-zimo/drive-storage-2026-08-30-5ef4    0 B
Feng-zimo/drive-storage-2026-08-30-80b9    0 B
```

这些仓库**当前不可见**（已删或权限已变），条目却永久留在 config 里。
金额都是 0 所以不影响容量，但每次同步都要带着它们传输。

---

## 五、★ 跨端契约冲突：minChunkSize

| 端 | minChunkSize | 后果 |
|---|---|---|
| 网页版（真实 config） | **10 MB** | 只有 >10MB 的文件才分片 |
| 桌面版 `config.py` 默认 | **512 KB** | >512KB 就分片 |

当前 15 个文件里，**8 个在 512KB~10MB 之间** —— 网页版下它们都是单片，
一旦桌面版写过 `storageConfig`，它们会被重新切成 2~16 片。

分片数从 136 会涨到 200+，**每片一次 API 调用**。而且要命的是：
已经上传的旧文件仍是单片，新上传的才分片 —— **同一批文件两种形态**，
后续按 chunk 列表下载时容易对不上。

这不是理论风险：`config_sync` 已经在写 `config.json` 了（0.0.2 起），
虽然目前只改 `fileIndex` 字段，但任何一次全量 `storageConfig` 写入都会触发。

**修法**：桌面版默认值改为与网页版一致（10 MB），并且**永远不写 `storageConfig`**
—— 它是网页版用户的既有设置，桌面版没有理由改写。

---

## 六、其他发现

**单文件 63.6 MB → 122 片。** 这是唯一真正需要分片的文件，
占全部分片的 90%。分片本身工作正常（`ceil(63582206/524288)=122` ✓ 精确吻合）。

**同名文件存了两份。** `…-hu15` 里有一个 png，VFS 里的同名文件分片却指向
`…-8696` —— 早期上传留下的旧副本没清理。

**`.trash` 里有目录记录但没有文件记录。** 回收站清空逻辑在 `file-manager.js`
里搜不到（`emptyTrash` 零匹配），可能是靠 `deleteFile` 递归实现的，
需要确认递归时是否真的逐片删了 GitHub 上的分片。

**分享 4 个，1 个是空壳**（`gd-share-mty7vf24` 仅 22 B）。
分享仓库是公开的，空壳留着只是占位。

---

## 七、建议的修复顺序

| 优先级 | 问题 | 动作 |
|---|---|---|
| **P0** | 桌面版 minChunkSize 冲突 | 默认值改 10MB + 永不写 storageConfig |
| **P0** | `subtractFromRepoUsage` 零调用 | 删除文件时真正扣减 |
| **P1** | 56 MB 孤儿数据 | 加"清理孤儿"工具：扫所有存储仓，对比 VFS，列出无主 blob |
| **P1** | 记账低估 | 加"校准"功能：用 Git tree API 重算真实大小，覆盖记账 |
| **P2** | 僵尸记账条目 | 同步时清理指向不存在仓库的条目 |
| **P2** | 分片删除失败静默 | 记录失败清单，可重试；不要只 console.warn |

---

## 八、方法论备注

这份报告里每一个数字都来自 API 实测，**没有一个是推算的**。

特别是"56.2 MB 孤儿"这个结论：是先从 VFS 取出全部分片路径，
再拉 4 个仓库的 Git tree 逐条比对得出的，不是看容量条猜的。

之前出过"凭印象写文档"的事故（仓鼠的游戏模块、误提交目录），
所以本次凡是写进来的结论，都先跑了 API 验证。

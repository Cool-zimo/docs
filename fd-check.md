# fd 检查法

代码改完之后必须走完的固定流程。**不是"检查三遍"，是三遍检查三种不同的东西。**

---

## 为什么要有这个

过去出过的事故，几乎都集中在同一类：**改完了，以为改对了，实际没生效，或者生效了但引入了新问题。**

典型三种：

1. **声称与实现不符** —— 注释/文档写着"已做 X"，代码里根本没有，或字段名写错
2. **测试没看向那个维度** —— 171 项全绿，但打包后启动即崩溃（测的是源码环境，不是产物环境）
3. **修 A 引入 B** —— 收紧一处规则，误杀了正当用法

fd 检查法就是针对这三种。

---

## 第 1 遍：静态逐模块审查

**视角：代码本身对不对。**

逐文件读源码，不是扫 grep。重点：

| 检查项 | 常见错误 |
|---|---|
| 控制流 | 判断写在 return 之后，永远走不到 |
| 变量 | 捕获组忘了加、`idm[1]` 是 undefined |
| 属性 vs 方法 | `it.isDirectory` 是方法，写成属性恒为真 → 全部误判 |
| 边界 | 空串、0、None、空容器 |
| 记账 | 加/减是否配对，扣减量写死 0 |

**产出**：一份问题清单，不急着改。

---

## 第 2 遍：契约与一致性

**视角：声称的 vs 实际的，以及模块之间是否对得上。**

这一遍最容易抓到"文档写了但代码没有"。

| 检查项 | 怎么查 |
|---|---|
| **注释/文档 vs 代码** | 把注释里声称的行为写成断言，跑一遍 |
| **两套判据** | 同一规则有没有被抄成两份？是不是已经漂移？ |
| **字段名** | 写入方和读取方的 key 是否一致 |
| **跨端契约** | 桌面版和网页版的命名/阈值/格式是否一致 |
| **安全判据唯一性** | 判据只能有一处，其余做调用 |

★ **判据漂移是本检查法最优先的一项。** 只要发现同一规则有两份实现，一律合并，不看"现在是否还一致"。

---

## 第 3 遍：失败路径与边界

**视角：出错时会怎样。**

| 检查项 | 典型漏洞 |
|---|---|
| **异常吞掉** | `except: pass` 掩盖真实原因 |
| **回滚是否真回滚** | 失败后记账/状态是否回到原点 |
| **资源耗尽** | 循环里建资源（仓库/文件/连接）有没有上限 |
| **编码** | 中文路径、非 ASCII URL |
| **提权/包装** | 安全规则能否被 `sudo` 之类前缀绕过 |
| **误杀** | 收紧规则后，正当用法是否还能用 |

★ 每一条收紧都必须配一条"不能误杀"的反向用例。黑名单太狠会废掉功能。

---

## 冒烟测试

**必须在"产物环境"跑，不只是源码环境。**

这是第 4 条经验教训的由来：源码下 `shim.js` 就在旁边，`open()` 必然成功，测试全绿；打包后文件不存在，启动即崩溃。

| 环境 | 怎么验 |
|---|---|
| 源码环境 | 跑测试套件 |
| **打包环境** | 真的把资源文件移走，模拟 `_MEI`，验证仍可用 |
| 失败路径 | 断言"不修复确实会崩"，证明这条测试有效 |

★ 测试要能**证明自己有效**：加一条"对照用例"，验证旧写法确实会失败。

---

## 报告

每条问题必须三要素齐全，缺一不可：

```
错误原因  —— 为什么会错，根因是什么（不是现象）
修改方法  —— 改了什么，为什么这么改（含被否决的方案）
版本号    —— 落在哪个版本
```

**现象和根因要分开写。** 症状往往和原因对不上：

| 症状 | 真实原因 |
|---|---|
| 文件列表永远为空 | prefix 处理错，子项被跳过 |
| 打包莫名失败 | Defender 把生成的 exe 当木马删了 |
| 启动即崩溃 | `.js` 没进 `--add-data` |
| 分享下载崩溃 | URL 含中文，http.client 用 ascii 编码 |

---

## 收尾检查（非代码项）

代码对了，产物未必对。这一遍**只看不写在代码里的东西**：

| 检查项 | 为什么 |
|---|---|
| **`--add-data` 资源** | PyInstaller 只自动收 `.py`，`.js`/`.json`/图标/static 全要显式声明 |
| **版本号格式** | CRLF 会让 `tr -d ' \n'` 残留 `\r`，文件名非法 |
| **文件名** | 是否带版本号、三平台是否一致 |
| **workflow** | 各平台是否独立（`if: !cancelled()`），一个挂了不卡全部 |
| **Pages / 发布** | 是否真的 built，产物是否真的存在 |
| **资源完整性** | GitHub API 不能真删数据，只有重建仓库才彻底清除 |

★ 漏 `--add-data` 的表现是**静默的**：exe 能起来，功能缺失，且不报错。这是最难查的一类。

---

## 执行清单

改完代码后：

```
[ ] 第 1 遍  静态逐模块审查
[ ] 第 2 遍  契约与一致性（含判据漂移）
[ ] 第 3 遍  失败路径与边界（含误杀反向用例）
[ ] 冒烟测试  源码环境 + 产物环境 + 对照用例
[ ] 报告      错误原因 / 修改方法 / 版本号
[ ] 收尾      --add-data / 版本号 / 文件名 / workflow / 发布状态
[ ] 交付      实际访问验证（我这边常被网关拦，需用户确认）
```

---

## 一条硬规则

**凡是要写进文档或报告的"现状"，先读源码确认，不查就不写。**

这条是代价最大的一条换来的：曾经凭印象写出一整套游戏模块（实际是仓库管理面板）、写出"误提交的嵌套目录"（实际不存在）——两次都是没核实就下笔。

留个标记：错误记录**不抹掉**，改完在文档里保留"早期版本曾写错"的说明。删掉它，下次还会犯。

# fd 检查法 v2

代码改完之后必须走完的固定流程。**不是"检查三遍"，是几遍检查几种不同的东西。**

> **v2 变更说明**：基于四次真实使用（tiny-md、gdpy 0.0.7、gdpy 0.0.8、
> Drive 全量检查）的体验重写。改动理由逐条记录在
> [fd-check-v2-notes.md](fd-check-v2-notes.md)。

---

## 为什么要有这个

过去出过的事故，几乎都集中在同一类：**改完了，以为改对了，实际没生效，或者生效了但引入了新问题。**

六种典型：

1. **声称与实现不符** —— 注释/文档写着"已做 X"，代码里根本没有，或字段名写错
2. **测试没看向那个维度** —— 171 项全绿，但打包后启动即崩溃（测的是源码环境，不是产物环境）
3. **修 A 引入 B** —— 收紧一处规则，误杀了正当用法
4. **只查了一个方向** —— grep `add` 得到 2 处，就推断 `sub` 零调用（实际有 3 处）
5. **代码对但数据烂** —— 逻辑正确，长期运行后线上数据已经脱节
6. **成功但不可用** —— 构建全绿、产物齐全，但 release 是草稿态，用户根本拿不到

v1 覆盖了前三种，v2 补上后三种。

---

## 第 1 遍：静态审查（限 diff 范围）

**视角：本次改动的代码本身对不对。**

★ **v2 改动：不再全量读源码。** 全量读在首次接手项目时才必要；
日常改动时大部分时间在读没动过的代码。

```
范围 = 本次改动的 diff
     + 所有调用方（谁 import / 引用了它）
     + 它依赖的常量定义处
```

不动的文件只在第 2 遍做契约层级检查，不逐行读。

| 检查项 | 常见错误 |
|---|---|
| 控制流 | 判断写在 return 之后，永远走不到 |
| 变量 | 捕获组忘了加、`idm[1]` 是 undefined |
| 属性 vs 方法 | `it.isDirectory` 是方法，写成属性恒为真 → 全部误判 |
| 边界 | 空串、0、None、空容器 |
| 记账 | 加/减是否配对，扣减量写死 0 |

**产出**：一份问题清单，不急着改。

---

## 第 2 遍：契约与一致性 ★ 性价比最高

**视角：声称的 vs 实际的，以及模块之间是否对得上。**

★ **这一遍在四次使用中每次都抓到了最严重的问题。** 投入产出比明显高于第 1 遍。

| 检查项 | 怎么查 |
|---|---|
| **注释/文档 vs 代码** | 把注释里声称的行为写成断言，跑一遍 |
| **两套判据** | 同一规则有没有被抄成两份？是不是已经漂移？ |
| **字段名** | 写入方和读取方的 key 是否一致 |
| **跨端契约** | 桌面版和网页版的命名/阈值/格式是否一致 |
| **安全判据唯一性** | 判据只能有一处，其余做调用 |

★ **判据漂移是本检查法最优先的一项。** 只要发现同一规则有两份实现，一律合并，不看"现在是否还一致"。

### ★★ 反向 grep 规则（v2 新增）

**v1 事故**：查记账时只 grep 了 `addToRepoUsage`（2 处），
没 grep `subtractFromRepoUsage` 就写报告说"零调用"——实际有 3 处调用，
结论完全错了方向。

```
凡查"有没有做 X"，必须同时查：
  - X 本身
  - X 的反操作（add/sub、open/close、lock/unlock、push/pull）
  - X 的别名与可选链写法（?. 会让 grep 结果不一样）
```

**对称操作只查一半 = 没查。**

---

## 第 3 遍：失败路径与边界

**视角：出错时会怎样。**

| 检查项 | 固定扫 |
|---|---|
| **异常吞掉** | `except: pass` / `catch(e){}` 掩盖真实原因 |
| **回滚是否真回滚** | 失败后记账/状态是否回到原点 |
| **资源耗尽** | 循环里建资源（仓库/文件/连接）有没有上限 |

★ **v2 改动**：上面 3 项固定扫，下面 3 项改为**动态选题**
（v1 固定扫全部 6 项，结果是打勾走过场——"编码"连续三次零发现）：

```
  编码       —— 本次改动涉及网络/文件路径时才扫
  提权/包装  —— 本次改动涉及权限/命令执行时才扫
  误杀       —— 本次收紧了任何规则时必须扫
  + 上轮报告里出现过的问题类别（复发检查）
```

★ 每一条收紧都必须配一条"不能误杀"的反向用例。黑名单太狠会废掉功能。

---

## 第 3.5 遍：线上数据核验（v2 新增，定期做）

**视角：代码是对的，但数据是不是烂的。**

★ **v1 完全没有这一遍，而它抓到了最大的问题：42% 孤儿数据。**

这一遍**看代码永远发现不了** —— 逻辑可能全对（上传加分片、删除删分片），
但长期运行后数据与记账已经脱节。单元测试也覆盖不到，因为测试用的是干净环境。

```
1. 拉真实配置文件（不是默认值、不是测试 fixture）
2. 拉真实存储仓 tree，统计实际占用
3. 比对：声明的记录 vs 实际的物理数据
4. 找三样东西：
   - 孤儿：物理存在但无记录（占容量、拿不回）
   - 幽灵：有记录但物理不存在（下载必失败）
   - 偏差：记账 vs 实际（低估会让容量判断失效）
```

★ **必须定期做，不是只在改代码后做。** 建议：功能检查时做，
另外每隔 N 个版本做一次。

---

## 冒烟测试：四层（v1 只有一层）

| 层 | 验什么 | 不做的后果 |
|---|---|---|
| **源码环境** | 跑测试套件 | — |
| **产物环境** | 打包后资源是否齐全 | 启动即崩溃（v0.0.4 那次） |
| **线上环境** | 真实数据是否自洽 | 静默错误，界面看不出来（42% 孤儿） |
| **发布环境** | release 是否真的可见（`draft == false`） | 构建全绿但用户下载不到（v0.0.8 那次） |

★ 后两层是 v2 新增的，**也都是踩过才加的**。

**产物环境怎么验**：真的把资源文件移走，模拟 `_MEI`，验证仍可用。
源码下资源就在旁边，`open()` 必然成功，测试全绿也证明不了什么。

★ 测试要能**证明自己有效**：加一条"对照用例"，验证旧写法确实会失败。

---

## 报告：四要素（v1 是三要素）

每条问题必须齐全，缺一不可：

```
错误原因      —— 为什么会错，根因是什么（不是现象）
修改方法      —— 改了什么，为什么这么改（含被否决的方案）
版本号        —— 落在哪个版本
★ 防复发断言  —— 留下了哪条测试断言（v2 新增）
```

**第四要素是 v2 加的。** 理由：修过的 bug 会回来——OAuth 删了之后文档里又冒出来，
`hidden` 被 `display:flex` 覆盖改完没留断言。**没有断言的修复等于没修。**

**现象和根因要分开写。** 症状往往和原因对不上：

| 症状 | 真实原因 |
|---|---|
| 文件列表永远为空 | prefix 处理错，子项被跳过 |
| 打包莫名失败 | Defender 把生成的 exe 当木马删了 |
| 启动即崩溃 | `.js` 没进 `--add-data` |
| 分享下载崩溃 | URL 含中文，http.client 用 ascii 编码 |
| 进度条不动 | 后台线程没 join，主线程已退出 |

---

## 收尾检查（非代码项）：必须给查法

代码对了，产物未必对。这一遍**只看不写在代码里的东西**。

★ **v2 改动：每一项给出具体查法，不只给检查项名称。**
v1 只有一个表格，打勾就过——VERSION 文件落后 6 个版本就是这么漏掉的。

| 检查项 | 具体怎么查 |
|---|---|
| **版本号一致** | 对比三处：`VERSION 文件` / `最新 release tag` / `代码里的 __version__`。**三者必须一致**（v0.0.8 时三者全不一样） |
| **资源 `--add-data`** | 脚本扫描所有非 `.py` 运行时文件，逐个比对是否已声明（v0.0.8 靠脚本发现 8 个未覆盖） |
| **发布可见** | 查 API 确认 `draft == false`。**构建成功 ≠ 用户能拿到** |
| 文件名 | 是否带版本号、三平台是否一致 |
| workflow | 各平台是否独立（`if: !cancelled()`），一个挂了不卡全部 |
| 资源完整性 | GitHub API 不能真删数据，只有重建仓库才彻底清除 |

★ 漏 `--add-data` 的表现是**静默的**：exe 能起来，功能缺失，且不报错。这是最难查的一类。

---

## 执行清单

```
[ ] 第 1 遍    静态审查（限 diff + 调用方）
[ ] 第 2 遍    契约一致性（含判据漂移 + 反向 grep）
[ ] 第 3 遍    失败路径（固定 3 项 + 动态选题）
[ ] 第 3.5 遍  线上数据核验（定期）
[ ] 冒烟       源码 / 产物 / 线上 / 发布 四层
[ ] 报告       原因 / 方法 / 版本 / 防复发断言
[ ] 收尾       每项按给定查法执行，不只打勾
[ ] 交付       实际访问验证（我这边常被网关拦，需用户确认）
```

---

## 一条硬规则（v2 已扩展）

> **凡是要写进文档或报告的"现状"：**
> **① 先读源码确认「意图」**
> **② 再查线上数据确认「事实」**
> **③ 两者不一致时以事实为准，并当成 bug 报出来**

**v1 只有第 ① 步。** v2 加 ②③ 是因为：源码是对的，线上数据可能是烂的。
读源码只能证明"代码想做什么"，不能证明"实际发生了什么"。

这条是代价最大的一条换来的：

- 凭印象写出一整套游戏模块（实际是仓库管理面板）
- 写出"误提交的嵌套目录"（实际不存在）
- 只 grep 一半就写"零调用"（实际有 3 处）
- 三个都是**没核实就下笔**

留个标记：错误记录**不抹掉**，改完在文档里保留"早期版本曾写错"的说明。
删掉它，下次还会犯。

# fd 检查法 · 用了三次之后的改进建议

**背景**：v1 已经实际用了三次（tiny-md、gdpy 0.0.7、gdpy 0.0.8），
加上本次对 Drive 的检查一共四次。下面是基于真实使用体验的判断，
不是理论推导 —— 每条都注明"为什么这么改"。

---

## 一、哪些部分太臃肿

### 1. 第 1 遍「静态逐模块审查」范围过大

**v1 写法**：「逐文件读源码，不是扫 grep」

**问题**：这次 Drive 检查里，第 1 遍逐文件读完只抓到 2 个问题，
而第 2 遍（契约一致性）抓到了最严重的 P0。**投入产出比明显不对**。

全量读源码在两种情况下才有必要：
- 第一次接手一个项目
- 改动触及核心数据结构

日常改动时全量读，大部分时间在读没动过的代码。

**建议改为**：

```
第 1 遍范围 = 本次改动的 diff
            + 所有调用方（谁 import 了它）
            + 它依赖的常量定义处
```

不动的文件只在"契约"层级检查（第 2 遍），不逐行读。

---

### 2. 第 3 遍的 6 项固定扫描有冗余

**v1 表格**：异常吞掉 / 回滚 / 资源耗尽 / 编码 / 提权 / 误杀

**问题**：「编码」这一项我每次都查，但自从 URL ascii 那次修完之后
**连续三次零发现**。固定扫全部六项，会变成走过场 —— 打勾就过。

**建议改为**：固定扫 3 项（异常吞掉、回滚、资源耗尽），
另外 3 项**按上一轮报告动态选题**：

```
本轮扫什么 = 固定 3 项
           + 上轮报告里出现过的问题类别（复发检查）
           + 本次改动涉及的领域（改了网络 → 扫编码）
```

---

## 二、哪些部分太简略

### 3. ★ 收尾检查只有表格，没有方法

**v1 写法**：一个 6 行表格，列了「`--add-data` / 版本号 / 文件名 / workflow / 发布状态」

**问题**：这次发现 **VERSION 文件停在 0.0.1，而已经发布到 0.0.7** ——
版本号倒退六个版本。这个 bug 表格里"版本号"那一行确实覆盖了，
但**表格没告诉我怎么查**，靠人肉想起去看 VERSION 文件才发现的。

清单式检查最容易失效的地方就是：打了勾，但其实没真查。

**建议**：每一项给出**具体的查法**，而不是只给检查项名称。

| 检查项 | v1 | v2 建议 |
|---|---|---|
| 版本号 | 「版本号格式」 | 对比 `VERSION 文件` / `最新 release tag` / `代码里的 __version__` **三者是否一致**（本次三者全不一样） |
| 资源 | 「`--add-data` 资源」 | 脚本扫描所有非 `.py` 运行时文件，逐个比对是否已声明（本次就靠脚本发现 8 个未覆盖） |
| 发布 | 「Pages / 发布」 | 查 API 确认 `draft == false`（本次 0.0.8 卡在草稿态，构建全绿但用户拿不到） |

**最后一条是本次最痛的**：构建成功、产物齐全、Pages built，
但 release 是草稿，用户从主页根本看不到。**"成功"和"可用"是两件事。**

---

### 4. ★★ 缺一整遍：线上数据核验

**这是本次最大的发现，而 v1 里根本没有这一项。**

「42% 孤儿数据」「记账低估 8MB」全部来自：
把 config.json 的 VFS 记录，与 4 个存储仓的真实 Git tree 逐条比对。

**看代码永远发现不了这类问题** —— 代码逻辑是对的（上传加分片、删除删分片），
问题出在**长期运行后数据与记账脱节**。单元测试也覆盖不到，
因为测试用的是干净环境。

**建议新增为独立一遍**：

```
线上数据核验
  1. 拉真实配置文件（不是默认值、不是测试 fixture）
  2. 拉真实存储仓 tree，统计实际占用
  3. 比对：声明的记录 vs 实际的物理数据
  4. 找三样东西：
     - 孤儿：物理存在但无记录（占容量、拿不回）
     - 幽灵：有记录但物理不存在（下载必失败）
     - 偏差：记账 vs 实际（低估会让容量判断失效）
```

★ 这一遍**必须定期做**，不是只在改代码后做。
建议：功能检查时做，另外每隔 N 个版本做一次。

---

### 5. 缺「回归护栏」：修过的 bug 会不会回来

**v1 完全没有这一项。**

实际发生过：OAuth 删了之后，文档里又冒出来；
`hidden` 被 `display:flex` 覆盖这类问题改完就没留断言。

**建议**：每个修过的 bug，在报告里标注**留下了哪条断言**。
报告格式从三要素扩成四要素：

```
错误原因 / 修改方法 / 版本号 / ★ 防复发断言在哪
```

没有断言的修复，等于没修 —— 下次有人改动又会撞回来。

---

### 6. 「冒烟测试」定义太窄

**v1 只写了「产物环境」（打包后资源缺失）**

**问题**：本次暴露的另外两类失败它都不覆盖：
- **线上环境**：打包没问题，但真实数据已经烂了（42% 孤儿）
- **发布环境**：产物没问题，但 release 是草稿（用户拿不到）

**建议扩成三层**：

| 层 | 验什么 | 不做的后果 |
|---|---|---|
| 源码环境 | 单元测试 | — |
| **产物环境** | 打包后资源是否齐全 | 启动即崩溃（v0.0.4 那次） |
| **线上环境** | 真实数据是否自洽 | 静默错误，界面看不出来（42% 孤儿） |
| **发布环境** | release 是否真的可见 | 构建全绿但用户下载不到（v0.0.8 那次） |

后两层是本次新增的，**也都是踩过才加的**。

---

## 三、v2 结构建议

```
第 1 遍  静态审查（限 diff + 调用方，不全量）
第 2 遍  契约一致性（含判据漂移）★ 性价比最高
第 3 遍  失败路径（固定 3 项 + 动态选题）
第 3.5 遍 线上数据核验（定期，非每次）★ 新增
冒烟测试  四层：源码 / 产物 / 线上 / 发布
报告      四要素：原因 / 方法 / 版本 / 防复发断言
收尾      每项给具体查法，不只给检查项名
```

---

## 四、一条原则性的补充

v1 结尾的硬规则是「凡是要写进文档的现状，先读源码确认」。

**本次说明这条还不够**：源码是对的，但**线上数据是烂的**。
读源码只能证明"代码想做什么"，不能证明"实际发生了什么"。

**建议扩成**：

> 凡是要写进文档的「现状」，
> 先读源码确认「意图」，
> 再查线上数据确认「事实」，
> 两者不一致时**以事实为准，并当成 bug 报出来**。

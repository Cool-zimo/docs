---
title: 整体架构
---

# 🏗️ 整体架构

三个应用是同一套架构哲学的不同实例。理解了这套哲学，三个代码库都能很快看懂。

---

## 一、根本约束：没有后端

```
┌─────────────────────────────────────────┐
│  浏览器（纯静态页面，GitHub Pages 托管）  │
│                                          │
│  localStorage  ← Token + 私钥            │
└──────────────┬───────────────────────────┘
               │ HTTPS
               ▼
┌─────────────────────────────────────────┐
│  GitHub REST API                         │
│  ├─ repos/contents   文件读写            │
│  ├─ issues/comments  消息流              │
│  └─ collaborators    权限                │
└─────────────────────────────────────────┘
```

**没有服务端、没有数据库、没有中间人。** 直接后果：

| 约束 | 后果 | 应对 |
|---|---|---|
| 无服务端 | 无法"推送" | 轮询（FaceHub 5s，AI 回复） |
| 无服务端 | 无法常驻 | 关掉页面就停 |
| localStorage 存私钥 | 换设备旧消息解不开 | 备份导出（明文） |
| GitHub API 限速 | 5000 次/小时 | ETag 条件请求 + 多级缓存 |
| contents API 单文件上限 | ~30MB 实测 | 分片上传 |

这套架构的价值是：**数据主权完全归用户**。代价是上面那张表。

---

## 二、数据存放模式

三个应用都用「仓库即数据库」，但用法不同：

| 应用 | 身份/配置 | 业务数据 |
|---|---|---|
| **FaceHub** | `facehub-{login}`（公开） | `fbdm-{A}-{B}` / `fhgrp-{owner}-{id}`（**私有**） |
| **GitHub Drive** | 配置在多仓库间同步 | `drive-storage-{date}-{rand}`（私有） |
| **仓鼠** | `cangshu-config`（私有） | 不存业务数据，只管别人的仓库 |

**关键设计**：FaceHub 的身份仓库是**公开**的。因为公钥本来就该让人读——任何人想给我发加密消息，直接去 `facehub-{login}/pk.json` 取即可，**我不需要在线**。

---

## 三、消息流：issue comment

FaceHub 的会话仓库是一个**私有仓库 + 一个 issue**，每条消息是一个 comment。

```
fbdm-cool-zimo-feng-zimo/
├── pk.json          双方身份公钥（其实在各自身份仓库）
└── issue #1
    ├── comment 1    E2E1.<iv>.<cipher>
    ├── comment 2    E2E1.<iv>.<cipher>
    └── comment 3    E2E1.<iv>.<cipher>
```

**为什么用 issue comment 而不是文件**：

- 文件每次修改都要带 `sha`，并发写必然冲突（409）
- comment 是**追加**语义，天然支持并发
- 自带时间戳和作者，不用额外维护
- `since` 参数支持增量拉取

代价：删除消息 = 删 comment，仍留在 Git 历史里（见[开发者手册](developer.md#git-历史的不可删除性)）。

---

## 四、缓存架构（FaceHub）

三层缓存，键名带前缀隔离：

| 前缀 | 存什么 | 失效时机 |
|---|---|---|
| `fh:etag:` | 目录列举的 ETag | 该仓库任意写操作 |
| `fh:e:` | 单文件 ETag | 该文件被写 |
| `fh:c:` | 文件内容（文本） | 该文件被写 |
| `fh:c:...\|b64` | 文件内容（base64） | 该文件被写 |

**条件请求**：带 `If-None-Match`，命中 304 **不计入配额**。这是省配额的核心手段。

### 一个真实的坑

```
writeFile 只清了 fh:etag:，没清 fh:c: 内容缓存
   ↓
写完立刻读 → 拿到写入前的旧值
   ↓
表现为"关注了但列表里没有"，刷新才好
```

这个 bug 只在「写入后 200ms 内立刻读」时复现，刷新后一切正常——极难排查。

**修法**：写入时三个 key 一起清（见 `api.js` 的 `writeFile`）。代码里有注释锁死，改动时别删。

---

## 五、跨应用互认（Bridge）

三个应用共用同一套身份仓库模式。`js/bridge.js` 声明：

```js
var APPS = [
  { key: 'facehub', path: '/FaceHub/',      name: 'FaceHub' },
  { key: 'drive',   path: '/github_drive/', name: 'GitHub Drive' },
  { key: 'cangshu', path: '/cangshu/',      name: '仓鼠' }
];
```

**一个容易写错的地方**：不能简单取"另一个应用"，必须找**确实已登录**的那个。

否则当 Drive 和仓鼠都没登录时，逻辑会指向一个空应用，提示错位。

Token 只在同源（`cool-zimo.github.io`）的三个路径间共享，不写公共存储。

---

## 六、模块依赖图（FaceHub）

```
        app.js（主应用，3828 行）
             │
    ┌────────┼────────┬────────┬────────┐
    │        │        │        │        │
  chat.js  moments  group.js attach.js ai-reply.js
    │        │        │        │
    └────────┴────────┴────────┘
             │
      ┌──────┴──────┐
   crypto.js      api.js
   (673 行)      (592 行)
      │              │
   store.js ─────────┘
```

- `crypto.js` 不依赖 `api.js` 以外的任何模块（纯加密）
- `api.js` 不认识业务概念，只做 REST 封装 + 缓存
- `store.js` 管身份仓库的初始化

依赖是**单向**的：上层可以调下层，下层不知道上层存在。

---

## 七、为什么不用框架

无构建、无依赖、无打包。直接 `<script>` 引入，IIFE 挂全局。

**理由**：

1. Pages 直接托管，不需要 build 步骤
2. 没有 node_modules，仓库体积小
3. 改完推上去就生效，没有构建产物和源码不同步的风险

代价：`app.js` 3828 行偏大。可接受——按功能分区注释清晰。

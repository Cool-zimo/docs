# fd 检查报告 · GitHub Drive（网页版 + 桌面版）

**检查日期**：2026-09-26
**对象**：`Cool-zimo/github_drive`（网页版）+ `Cool-zimo/gdpy`（桌面版）
**方法**：fd 检查法（静态 / 契约一致性 / 失败路径 / 冒烟 / 收尾）

---

## 第 1 遍 · 静态审查

| # | 位置 | 问题 | 级别 |
|---|---|---|---|
| 1 | `storage.js:703` | `subtractFromRepoUsage()` 定义了但**全项目零调用** | 高 |
| 2 | `file-manager.js:434` | 分片删除失败只 `console.warn`，无重试、无记录 | 中 |

**① 记账只增不减**

```javascript
// 定义存在
subtractFromRepoUsage(owner, repo, sizeBytes) { ... }

// 调用点搜索
addToRepoUsage       → 2 处（定义 + file-manager.js:173 上传时）
subtractFromRepoUsage → 0 处   ← 没有任何地方调用
```

上传加、删除不减。实测后果：某仓库记账 44.6 MB 而实际 40.9 MB。

**② 删除失败静默**

```javascript
} catch (e) {
    console.warn(`[FileManager] 删除分片失败: ${chunk.path}`, e.message);
}
```

`sha` 过期、限流、网络抖动都会让分片删不掉，然后无声留下。
这是"56 MB 孤儿数据"的直接成因之一。

---

## 第 2 遍 · 契约一致性

| # | 问题 | 级别 |
|---|---|---|
| 3 | **minChunkSize 跨端冲突**（网页版 10MB vs 桌面版 512KB） | **P0** |
| 4 | 文档模块行数表全部过期 | 低 |
| 5 | 文档只写 `chunkSize`，未提 `minChunkSize` | 中 |

### ★ ③ minChunkSize（最严重）

```
网页版真实 config.json :  minChunkSize = 10485760  (10 MB)
桌面版 config.py:33    :  'minChunkSize': DEFAULT_CHUNK   (= 512 KB)
```

这个参数决定"多大的文件才分片"。桌面版默认值与线上实际值**差 20 倍**。

后果不是"慢一点"：

- 当前 15 个文件里 **8 个在 512KB~10MB**，网页版下全是单片
- 桌面版一旦写 `storageConfig`，这些文件会被切成 2~16 片
- 分片总数 136 → 200+，每片一次 API 调用
- **更糟**：旧文件仍是单片，新文件是分片 —— 同一批数据两种形态

**为什么这个必须算 P0**：`config_sync` 从 0.0.2 起就在写 `config.json`。
目前只改 `fileIndex`，但只要有一次写全量 `storageConfig`，用户的分片策略就被改掉了，
而且**不会有任何提示**。

**修法**（两条都要）：
1. 桌面版默认值改成 10 MB，与线上对齐
2. 桌面版**永不写 `storageConfig`** —— 那是网页版用户的既有设置

### ④⑤ 文档过期

| 文档声称 | 实际 |
|---|---|
| `ui.js` 2793 行 | 2748 |
| `app.js` 1911 行 | 1900 |
| `i18n.js` 836 行 | 814 |
| `extension.js` 183 行 | 182 |

全部是旧版本数字。另外文档的分片配置一节只写了 `chunkSize: 512KB`，
**没写 `minChunkSize`** —— 而后者才是决定"哪些文件被分片"的参数。

---

## 第 3 遍 · 失败路径与边界

| # | 问题 | 级别 |
|---|---|---|
| 6 | 记账低估会让 `pick_repo` 持续写入已接近上限的仓库 | 高 |
| 7 | `repoUsage` 有 5 条指向已不存在仓库的僵尸条目 | 低 |
| 8 | `.trash` 清空逻辑在 `file-manager.js` 里搜不到 | 待确认 |

### ⑥ 低估比虚高危险

实测 4 个仓库记账与实际对比：

| 仓库 | 记账 | 实际 | 偏差 |
|---|---|---|---|
| `…-8696` | 76.4 MB | 77.1 MB | −0.7 MB |
| `…-c4aa` | 44.6 MB | 40.9 MB | +3.7 MB |
| `…-ft3j` | 6.2 MB | 14.2 MB | **−8.0 MB** |
| `…-hu15` | 0.5 MB | 1.1 MB | −0.6 MB |

`pick_repo()` 靠记账判断剩余空间。ft3j 记账 6.2 MB 实际 14.2 MB，
系统**以为还空着 894 MB**。低估不会报错，只会一直写，
直到某次 API 返回超限才暴露 —— 那时已经写了一堆。

**虚高**成因清楚（删了不扣减）；
**低估**成因是记账条目在历史上丢过（config 重置 / 迁移 / 多设备覆盖）。

### ⑦ 僵尸条目

`repoUsage` 10 条 vs `repos` 5 条。差的 5 条全是 `Feng-zimo` 的存储仓，
当前已不可见。金额都是 0，不影响容量，但每次同步都要传输。

---

## 冒烟测试

本次为**线上数据核验**，不是代码单元测试。方法：

1. 拉 `github-drive-config` 的 `config.json`（47,582 字节）
2. 遍历 `fileIndex.files` 取出全部 136 个分片的 `owner/repo/path`
3. 对 4 个存储仓分别调 Git tree API（`recursive=1`）取真实 blob 列表
4. 逐条比对

结果：

```
VFS 记录的文件总量        77.0 MB
4 个仓库实际 blob 总量   133.3 MB
无 VFS 记录的孤儿        56.2 MB  (42%)
```

★ 这个结论是**比对出来的，不是推算的**。看容量条永远发现不了，
因为容量条本身用的就是错的那份记账。

---

## 收尾检查

| 项 | 状态 |
|---|---|
| config.json 字段名 `fileIndex` | ✓ 桌面版已兼容（0.0.2 修） |
| 读写保留未知字段 | ✓ 读-改-写已实现 |
| 409 SHA 重试 | ✓ 已实现 |
| 分享密码残留 | ✓ 已清除（含旧数据自动清理） |
| OAuth 残留 | ✓ 代码与文档均已移除 |
| **minChunkSize 对齐** | **✗ 未修** |
| **记账扣减** | **✗ 未修** |

---

## 修复清单

| 优先级 | 问题 | 修改方法 | 版本 |
|---|---|---|---|
| **P0** | minChunkSize 跨端冲突 | 桌面版默认改 10MB + 永不写 storageConfig | 待发 |
| **P0** | 记账只增不减 | 删除文件时调用 `subtractFromRepoUsage` | 待发 |
| P1 | 56 MB 孤儿 | 加"清理孤儿"工具（扫 blob 比对 VFS） | 待发 |
| P1 | 记账低估 | 加"校准"（Git tree 重算覆盖记账） | 待发 |
| P2 | 僵尸记账 | 同步时剔除非可见仓库 | 待发 |
| P2 | 删片失败静默 | 记录失败清单可重试 | 待发 |
| P2 | 文档行数过期 | 按实际更新 + 补 minChunkSize 说明 | 待发 |

# 数据清理报告 · 44 个孤儿文件的处置

## 结论

| 指标 | 清理前 | 清理后 |
|---|---|---|
| VFS 文件 | 15 个 / 77.0 MB | **20 个 / 109.1 MB** |
| 仓库实际占用 | 133.3 MB | **109.1 MB** |
| **孤儿** | **45 个 / 56.2 MB** | **0** |

省下 24.2 MB，且**记账与实际终于一致**（此前记账 77 MB、实际 133 MB）。

---

## 一、分类决策

44 个文件逐个人工判断，不是靠规则批量删。

### 删除 39 个（24.2 MB）

| 类别 | 数量 | 判断依据 |
|---|---|---|
| 凭据 / 密码 | 7 | GitHub 全权限 token ×2、智谱 API key、邮箱授权码、outlook 密码、保险箱密码 |
| 账号截图 | 10 | 163 / gitee / jilab / 华为 / ali / github 等账号信息截图 |
| 冯希钦系列副本 | 11 | **原位 VFS 已有完整副本**，这些是覆盖上传留下的旧版本 |
| 重复旧版本 | 5 | `save.json` ×4、`math_history.json` ×3 中较旧的几份 |
| 安装包 / 无用 | 4 | `quark.exe`、`python.exe`、`a.txt`、`untitled.txt` |

### 保留 5 个（32.0 MB）并归位

| 文件 | 去向 |
|---|---|
| 义务教育教科书 · 数学六年级上册.pdf (18.3 MB) | `/drive_home/` |
| （备份）义务教育教科书·语文六年级下册.pdf (13.7 MB) | `/drive_home/` |
| 歌曲包.png / fpvsim.png / crisp.png | `/drive_home/杂项截图/` |

---

## 二、两个判断点值得记录

### ① 「重复」不能只看文件名，要比时间戳

`save.json` 有 4 份、`math_history.json` 有 3 份。文件名被去重加过序号
（`save(1).json`），**看不出谁新**。

从 chunk path 的 `mt` 前缀（`Date.now().toString(36)`）反解出真实上传时间：

```
原位 /drive_home/games/snake/save.json   mtz8do3k  09-13 03:04:48  ← 最新
恢复 save(3).json                        mtz8crm2  09-13 03:04:42
恢复 save(2).json                        mtcpwy9n  08-28 08:56
恢复 save(1).json                        mtcpwk6o  08-28 08:56
恢复 save.json                           mtbqjluy  08-27 16:26  ← 最旧
```

**原位那份才是最新的**，所以连"看起来最新"的 `save(3)` 也一起删了。
只按文件名保留一份会留下错误版本。

### ② 冯希钦系列全是重复，不是隐私泄露

第一眼看到 10 个"冯希钦罪行"相关文件，直觉是要删。
**但先查了原位 VFS** —— 那 9 个在原位都在（含 6.81 MB 视频），
孤儿只是覆盖上传留下的旧副本。

如果没查就直接按"隐私"删，会误以为自己丢过数据。
反过来，如果按"孤儿=丢失"全恢复，就等于把 10 份重复塞回界面。

---

## 三、删除过程中犯的错

### 第一版删除脚本：39 个全部「失败」

```
批1 ok=0 skip=3 fail=10
```

当时我差点下结论"API 不给删"。**实际是 URL 编码写错了**：

```python
path.replace(' ', '%20')        # ✗ 只替换空格，中文原样留在 URL 里
urllib.parse.quote(path, safe='/')   # ✓ 中文正确编码
```

改成 `quote` 后立刻 200。

### 更隐蔽的问题：统计口径把失败伪装成「已不存在」

脚本把 `404` 一律计为 `skip`（"已不存在"）。
于是**编码错误导致的 404 也被算成"本来就删过了"**，
表面看是"清理干净了"，实际一个都没删。

正确做法：先 GET 拿真实 sha，404 才说明真不存在；
DELETE 返回非 2xx 必须计入 fail 并打印出来。

### 验证方式

删除后重新扫了一遍全量 tree 比对 VFS：

```
二次确认：27 个孤儿中，被 VFS 引用的有 0 个   ← 删前再核一次
删除 26 个（7.27 MB）| 已不存在 1 | 失败 0
★ 剩余孤儿 0 个
```

**删除前二次确认「不被 VFS 引用」这一步不能省** ——
孤儿判定依赖 VFS 完整，而 VFS 本身可能残缺。

---

## 四、⚠️ 必须做的事：轮换凭据

**删文件 ≠ 消除泄露。** 三处残留：

1. **Git 历史** —— Contents API 删除只改当前 tree，历史 commit 里的 blob 仍在，
   通过 commit SHA 可访问
2. **本次对话** —— 部分 token 明文在对话里出现过
3. **删除之前的任何时刻** —— 这些凭据已经在私有仓库里躺了数周

需要轮换：

- [ ] GitHub Personal Access Token（Cool-zimo）
- [ ] GitHub Personal Access Token（Feng-zimo）
- [ ] 智谱 AI API Key
- [ ] 163 邮箱授权码
- [ ] Outlook 密码
- [ ] 保险箱密码

私有仓库不等于安全——任何拿到 token 的人都能读。

---

## 五、根因：为什么会有 45 个孤儿

每次上传生成新目录 `mt<base36时间戳>/文件名`，
覆盖同名文件时 **VFS 只更新指针，旧目录永不清理**。

已在 `js/file-manager.js` 修：新文件上传成功后才清理旧分片
（顺序不能反，先删旧的而新上传失败 = 文件彻底没了），
清理失败只 warn 不抛异常（主流程已成功，不该因此报"上传失败"）。

**但存量孤儿仍需手工清理** —— 这就是为什么要有 `js/maintain.js`。
现在已经能自助扫描，不用再靠脚本。

---
title: GitHub Drive
---

# 📁 GitHub Drive

> 把 GitHub 当网盘用：上传、下载、分享，大文件自动分片。

- 在线：<https://cool-zimo.github.io/github_drive/>
- 仓库：`Cool-zimo/github_drive`

---

## 它怎么存

文件存进**私有仓库**，按容量自动新建：

```
drive-storage-2026-08-30-8696
drive-storage-2026-08-27-c4aa
...
```

单个仓库接近容量上限时就再开一个。文件名带时间戳，避免冲突。

**大文件自动分片**（默认阈值 200MB）。分片必须**串行上传**。

---

## ⚠️ 分片不能并发（实测）

曾经试过并发上传 5 个分片：

```
并发 5 个不同路径 → 3 成功 / 2 失败
失败原因: 409 "is at ab1cabd... but expected ..."
```

**即使路径不同也会冲突** —— 冲突发生在 commit 层面，不是文件层面。
每次 commit 都会基于仓库当前的 HEAD，并发提交会互相覆盖。

所以：**写入串行，读取并发**。

---

## 分享

生成一个 `gd-share-*` 仓库，里面放分享元数据。别人拿到链接就能下载，
不需要你的 Token。

---

## 令牌权限

需要 `repo`。文件都是私有仓库，读写都要完整权限。

---

## 已知限制

- GitHub 单文件上限 100MB（所以才要分片）
- 单仓库建议不超过 1GB
- API 限速 5000 次/小时，批量操作要注意
